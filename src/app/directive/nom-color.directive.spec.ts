import { NomColorDirective } from './nom-color.directive';

describe('NomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NomColorDirective();
    expect(directive).toBeTruthy();
  });
});
